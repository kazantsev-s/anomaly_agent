# Агент по поиску аномалий

## Предметная область

Данные были собраны с платформы [Kolesa.kz](https://kolesa.kz) - крупнейшей казахстанской онлайн-площадки автообъявлений по продаже авто. Основная доля объявлений - частные, чаще б/у. Количество объявлений достаточно большое, платформе важно, чтобы они были настоящими и корректно заполненными, чтобы пользователи в ленте видели как можно больше качественных объявлений, что повысило бы статус площадки, а продавцу важно как можно скорее найти покупателя.

В связи с этим агент будет искать среди опубликованных объявлений подозрительные, которые могут свидетельствовать о мошенничестве или некорректном заполнении данных.

## Реализация проекта

Проект представляет из себя AI-агента, который имеет доступ к данным, проводит полный анализ по поиску аномалий и умеет обрабатывать отдельные запросы пользователя. Для ответа на его вопросы и понимания представленных данных агент умеет формировать и выполнять SQL-запросы к базе данных, как заранее заготовленные, так и полностью кастомные, что является обязательным этапом при обработке запроса.

В проекте есть два режима работы:

- ответ на пользовательский вопрос через `/ask`;
- полный анализ аномалий таблицы через `/analyze`.

### Взаимодействие с агентом

Общение с агентом выстраивается через Telegram-бота, в котором работают следующие команды:

- `/start` - показывает краткое приветствие и подсказывает проверить подключение к базе.

- `/health` - проверяет доступность БД.

- `/ask {запрос}` - описание запроса для AI-агента (`/ask` и после пробела любой текстовый запрос).

- `/analyze` - запускает полный анализ таблицы по умолчанию: агент собирает схему и профиль данных, выполняет базовые проверки аномалий, при необходимости запускает дополнительные SQL-проверки и возвращает итоговый отчет.

- `/analyze {table_name}` - запускает полный анализ конкретной таблицы. Например, `/analyze kolesa_test_easy_001` позволяет проверить тестовый датасет с заранее внесенными аномалиями. Для тестовой таблицы после итогового отчета бот дополнительно считает качество поиска и отправляет отдельное сообщение с метриками.

### Демонстрационный агент

Telegram-бот, через которого реализуется взаимодействие с демонстрационным агентом (требует запущенных контейнеров на стороне хоста):

[@anomalyagent_bot](t.me/anomalyagent_bot)

### Используемый технологический стек

Проект написан на `Python`. В качестве СУБД используется `PostgreSQL`, запуск приложения и базы данных организован через `Docker` и `Docker Compose`.

Для работы с PostgreSQL используется асинхронная библиотека `asyncpg`: она отвечает за подключение к базе данных, инициализацию таблицы, загрузку данных из CSV и выполнение SQL-запросов.

Telegram-бот реализован с помощью библиотеки `aiogram`. Бот обрабатывает команды пользователя, проверяет подключение к базе данных и передаёт запросы AI-агенту.

Взаимодействие с LLM реализовано через официальный клиент `openai`, в частности через асинхронный клиент `AsyncOpenAI` и `Responses API`.

Логика AI-агента построена с использованием `LangGraph`. В проекте есть два графа:

- `ask_graph` - граф для команды `/ask`: генерация плана SQL-запросов, последовательное выполнение SQL и подготовка ответа пользователю.
- `analyze_graph` - граф полного анализа для команды `/analyze`: загрузка схемы, профилирование таблицы, сохранение прохода анализа, запуск проверок, дополнительные SQL-проверки, финальный отчет и оценка качества на тестовых датасетах.

Для загрузки переменных окружения используется `python-dotenv`.

### Логика работы графов

В проекте разделены две задачи: ответ на конкретный вопрос пользователя и полный анализ аномалий. Обе задачи собраны как графы `LangGraph`, где каждая нода отвечает за отдельный этап обработки.

`ask_graph` используется для команды `/ask`:

- `generate_sql_queries` - получает вопрос пользователя и формирует план из одного или нескольких SQL-запросов к таблице `kolesa`.
- `execute_sql` - последовательно выполняет запросы из плана и сохраняет результаты в состоянии графа.
- `answer` - передает исходный вопрос, выполненные SQL-запросы и их результаты агенту, чтобы получить итоговый ответ для пользователя.

После выполнения каждого SQL-запроса граф либо переходит к следующему запросу, либо формирует ответ:

```text
START -> generate_sql_queries -> execute_sql
                                 ├──> execute_sql
                                 └──> answer -> END
```

`analyze_graph` используется для команды `/analyze`:

- `load_schema` - берет имя таблицы из состояния графа, загружает фиксированную схему таблицы `kolesa` и раскладывает колонки по типам.
- `profile_table` - собирает профиль данных: количество строк, пропуски, уникальные значения, числовые статистики, частые текстовые значения и распределения boolean-полей. После этого профиль сохраняется в БД, а для анализа создается запись прохода.
- `run_standard_checks` - запускает базовые проверки: пропуски, `unknown`, пустые строки, числовые выбросы, невозможные годы, редкие или неожиданные категории, дубликаты и несогласованные поля.
- `plan_custom_checks` - получает компактный профиль, найденные проблемы и результаты уже выполненных custom SQL, после чего решает, нужны ли дополнительные SQL-проверки. Для каждого запроса агент указывает тип результата: поиск кандидатов в аномалии или контекст для объяснения найденных проблем.
- `run_custom_checks` - выполняет предложенные агентом read-only SQL-запросы и сохраняет их результаты или ошибки выполнения.
- `final_anomaly_answer` - сохраняет найденные findings, собирает стандартные findings, результаты custom SQL и формирует итоговый отчет по аномалиям. После успешного отчета проход закрывается со статусом `completed`, при ошибке - со статусом `failed`.
- `evaluate_analysis` - для тестовых таблиц сопоставляет ожидаемые аномалии с findings агента, сохраняет результат в таблицы оценки и готовит короткое сообщение с метриками.

После `plan_custom_checks` граф либо переходит к финальному отчету, либо запускает дополнительные SQL-проверки. Цикл `plan_custom_checks -> run_custom_checks -> plan_custom_checks` ограничен настройками из `.env`, чтобы анализ не выполнял бесконечное число запросов.

```text
START -> load_schema -> profile_table -> run_standard_checks -> plan_custom_checks
                                                            ├──> final_anomaly_answer -> evaluate_analysis -> END
                                                            └──> run_custom_checks -> plan_custom_checks
```

### Структура проекта

```text
anomaly_agent/
├── data/
│   ├── anomaly_cases/
│   │   ├── expected_anomalies_easy_001.json
│   │   ├── expected_anomalies_hard_001.json
│   │   ├── expected_anomalies_load.sql
│   │   ├── expected_anomalies_logic_001.json
│   │   ├── expected_anomalies_mixed_001.json
│   │   ├── kolesa_test_easy_001.csv
│   │   ├── kolesa_test_hard_001.csv
│   │   ├── kolesa_test_logic_001.csv
│   │   └── kolesa_test_mixed_001.csv
│   └── kolesa.csv
├── notebooks/
│   └── graph_visualization.ipynb
├── src/
│   ├── agent/
│   │   ├── analyze_graph.py
│   │   ├── analyze_nodes.py
│   │   ├── ask_graph.py
│   │   ├── ask_nodes.py
│   │   ├── helpers.py
│   │   ├── prompts/
│   │   │   ├── analyze/
│   │   │   │   ├── anomaly_report_system.prompt.md
│   │   │   │   ├── custom_check_planner_system.prompt.md
│   │   │   │   └── evaluation_matcher_system.prompt.md
│   │   │   ├── ask/
│   │   │   │   ├── answer_system.prompt.md
│   │   │   │   ├── answer_user.prompt.md
│   │   │   │   └── sql_system.prompt.md
│   │   │   └── common/
│   │   │       └── kolesa_fields.prompt.md
│   │   ├── state.py
│   │   └── tools.py
│   ├── bot/
│   │   └── main.py
│   ├── db/
│   │   └── postgres.py
│   ├── config.py
│   └── logger.py
├── .env.example
├── .gitignore
├── Dockerfile
├── README.md
├── docker-compose.db.yaml
├── docker-compose.yaml
├── requirements.txt
└── db.sql
```

Ноутбук `notebooks/graph_visualization.ipynb` используется для визуализации `ask_graph` и `analyze_graph`. Его можно прогнать отдельно, но запуск с актуальными версиями графов уже сохранен в репозитории.

### Структура базы данных

Схема базы данных описана в `db.sql`. При старте приложения `init_db` создает таблицы, загружает основной CSV `data/kolesa.csv`, затем загружает тестовые CSV из `data/anomaly_cases/` и ожидаемые аномалии из `expected_anomalies_load.sql`.

Основная таблица:

- `kolesa` - базовая таблица объявлений. В ней хранятся поля объявления с Kolesa.kz: идентификатор, ссылка, дата парсинга, марка, модель, поколение, год, город, тип кузова, топливо, объем двигателя, пробег, коробка передач, привод, руль, цвет, регистрация, количество фото, цена и информация о найденном изображении.

Тестовые таблицы устроены так же, как `kolesa`, но содержат специально измененные данные:

- `kolesa_test_easy_001` - простые явные ошибки в значениях.
- `kolesa_test_logic_001` - логические конфликты между связанными полями.
- `kolesa_test_hard_001` - сложные контекстные аномалии, где значение может быть нормальным само по себе, но странным для конкретной модели, года или типа авто.
- `kolesa_test_mixed_001` - смешанный набор простых, логических и контекстных аномалий.

Таблицы для описания тестов:

- `test_datasets` - справочник тестовых датасетов: `test_id`, имя таблицы, сложность, пути к CSV и JSON с ожидаемыми аномалиями.
- `expected_anomalies` - список аномалий, которые были внесены в тестовые данные вручную. Здесь хранится тип аномалии, измененные колонки, старые и новые значения, селектор строки и описание ожидаемого обнаружения.

Таблицы для сохранения запусков анализа:

- `analysis_profiles` - снимок профиля таблицы на момент запуска анализа: количество строк, схема и рассчитанный профиль по колонкам.
- `analysis_runs` - история проходов агента. Запись связывается с `test_id`, если анализируется тестовая таблица, и с `profile_id`, чтобы потом можно было восстановить контекст запуска.
- `agent_findings` - найденные агентом аномалии. Для findings с конкретными примерами сохраняются `row_id` и `kolesa_id`, а агрегатная информация остается в `evidence` и `raw_finding`. Сюда попадают стандартные findings и успешные custom SQL-результаты типа `anomaly_candidates`, сохраненные как `custom_sql_anomaly`. Результаты типа `context` используются для объяснения аномалий и сохраняются в metadata прохода, но не создают отдельные findings.

Таблицы для последующей оценки качества:

- `evaluation_results` - итоговые метрики сравнения найденных аномалий с ожидаемыми: recall внутреннего поиска, recall итогового отчета, количество пропусков и findings вне тестовой разметки, которые не участвуют в recall.
- `anomaly_detection_matches` - связи между конкретными ожидаемыми аномалиями и findings агента, а также отметки о том, показана ли проблема в итоговом отчете.

### Тестовые датасеты

Тестовые датасеты лежат в `data/anomaly_cases/`. Для каждого набора есть CSV с измененными объявлениями и JSON с ожидаемыми аномалиями. SQL-файл `expected_anomalies_load.sql` загружает описания этих аномалий в таблицы `test_datasets` и `expected_anomalies`.

`easy_001` (`kolesa_test_easy_001`) содержит 7 явных аномалий, которые должны находиться стандартными проверками:

- невозможный будущий год выпуска;
- отрицательный пробег;
- нулевая цена;
- несуществующий тип топлива;
- пустая марка автомобиля;
- отрицательный объем двигателя;
- дублирующийся `kolesa_id`.

`logic_001` (`kolesa_test_logic_001`) содержит 5 логических конфликтов:

- `found_img = true`, но ссылка и имя файла изображения пустые;
- `found_img = false`, но ссылка на изображение заполнена;
- `imgs_count = 0`, но изображение считается найденным;
- бензиновый автомобиль с нулевым объемом двигателя;
- электрический автомобиль с ненулевым объемом двигателя.

`hard_001` (`kolesa_test_hard_001`) содержит 6 контекстных аномалий:

- Lexus LX 570 с объемом двигателя 1.5 л;
- Lexus LX 470 с кузовом хэтчбек;
- Hyundai Sonata 2026 года с пробегом 300000 км;
- BMW i3 как бензиновый автомобиль с объемом 2.0 л;
- Toyota Camry 2011 с ценой 45 млн тенге;
- Toyota Land Cruiser с передним приводом.

`mixed_001` (`kolesa_test_mixed_001`) содержит 9 аномалий из разных групп:

- невозможный будущий год;
- отрицательный пробег;
- несуществующий тип топлива;
- два конфликта по изображениям;
- Lexus LX 570 с маленьким объемом двигателя;
- Hyundai Sonata 2026 года с большим пробегом;
- BMW i3 как бензиновый автомобиль;
- Toyota Camry 2011 с сильно завышенной ценой.

Для проверки конкретного тестового набора можно передать имя таблицы в команду:

```text
/analyze kolesa_test_easy_001
```

При таком запуске проход сохранится в `analysis_runs`, профиль таблицы - в `analysis_profiles`, а найденные проблемы - в `agent_findings`. Если таблица есть в `test_datasets`, проход дополнительно будет связан с ее `test_id`, а после финального отчета запустится оценка качества.

## Примеры работы

Ниже представлены кейсы работы агента - запрос в бот и итоговый ответ/отчет

### Ответ на запрос `/analyze`

В бот была просто отправлена команда `/analyze`, на которую агент сформировал ответ:

```text
1. Краткое резюме

Проверено 5396 строк объявлений. Базовые проверки нашли 9 групп находок. Главные реальные ошибки данных связаны с полями изображений. Остальные находки — статистические выбросы или кандидаты в аномалии, которые требуют ручной проверки.

Custom SQL не нашел расхождений между годом выпуска и диапазоном в generation. После корректировки значения топлива на электрический ошибок согласованности fuel_type и engine_volume не найдено.

2. Основные проблемы

• Ошибки в данных по изображениям:
80 строк: found_img=false, но img_url заполнен.
76 строк: количество фото больше 0, но found_img=false.
68 строк: количество фото равно 0, но found_img=true.

Примеры:
id=252, kolesa_id=222609623, Audi A4 1996 — ссылка
id=287, kolesa_id=212133908, Audi TT 2000 — ссылка
id=29, kolesa_id=219403477, Chevrolet Tahoe 2026, количество фото 0, но фото найдено — ссылка

• Кандидаты в аномалии по цене внутри группы brand+model+year:
id=448, kolesa_id=222075136, Hyundai Elantra 2023: цена 2300000, медианная цена группы 8400000, отношение к медиане 0.27 — ссылка
id=2766, kolesa_id=221871558, Kia K5 2021: цена 5000000, медианная цена группы 11500000, отношение к медиане 0.43 — ссылка

3. Детальные находки

Статистические IQR-выбросы:
• 441 строк по цене: цена выше верхней границы 27600000. Примеры Chevrolet Tahoe/Suburban выглядят как дорогие, но потенциально валидные объявления.
• 431 строк по объему двигателя: значения вне диапазона 0.5–4.5 литра. Большие объемы у Tahoe/Suburban могут быть валидными.
• 192 строки по количеству фото: больше верхней границы 39.5 фото, это скорее редкие насыщенные объявления, не обязательно ошибка.
• 70 строк по пробегу: пробег выше 501917.625 км.
• 3 строки по году выпуска: старые автомобили 1977–1983 годов, это может быть валидный редкий случай.

Кандидаты в аномалии по пробегу:
id=2100, kolesa_id=211009325, Audi A6 1997: пробег 196 км, возраст 30 лет — ссылка
id=3506, kolesa_id=217597431, BMW 520 1992: пробег 300 км, возраст 35 лет — ссылка
Также есть шаблонные пробеги вроде 999999, 777777, 555555; пример: id=269, kolesa_id=220658975, Audi A6 1995, пробег 999999 км — ссылка

Кандидаты по редким характеристикам:
• Тип кузова: Kia Cerato Coupe и Toyota Corolla Универсал похожи на валидные редкие категории; Santa Fe как Внедорожник при доминирующем Кроссовер может быть вопросом классификации.
• Объем двигателя: Lexus RX 350 2025 с объемом 2.5 при доминирующем 3.5 — кандидат на проверку модификации.
• Привод: Lexus LX 570 с Передний привод выглядит подозрительно; Toyota Sienna Полный привод и Sprinter Полный привод могут быть валидными редкими модификациями.

4. Что проверить вручную

1. Логику заполнения found_img, img_url, imgs_count.
2. Слишком дешевые объявления относительно медианы своей модели и года.
3. Пробеги-заглушки и крайне низкие пробеги у старых автомобилей.
4. Редкие сочетания привода, кузова и объема двигателя по конкретным ссылкам.

5. Итоговая оценка качества данных

Качество данных среднее: ключевые поля в целом пригодны, но блок изображений требует исправления, а поля цены, пробега и технических характеристик нуждаются в ручной валидации по кандидатам из custom SQL.
```

### Ответ на запрос `/analyze` тестовой таблицы

В бот была просто отправлена команда `/analyze kolesa_test_mixed_001` - таблица с различными внесенными аномалиями. Ответ агента (отчет, сообщение о старте расчета качества, отчет с оценкой качества поиска аномалий):

```text
1. Краткое резюме

Базовые проверки нашли аномалии, в том числе реальные ошибки данных: отрицательный пробег, невозможный год выпуска, неожиданное значение топлива и несогласованность полей по изображениям. Custom SQL дополнительно выявил кандидатов в аномалии по цене, пробегу, поколению, кузову, объему двигателя и приводу.

2. Основные проблемы

Реальные ошибки данных:
- mileage: найден 1 отрицательный пробег.
- year: найден 1 невозможный год выпуска.
- fuel_type: найдено неожиданное значение пар в 1 объявлении.
- Изображения: есть расхождения между found_img, img_url и imgs_count.

Кандидаты в аномалии по custom SQL:
- цена сильно отличается от медианы для той же группы brand+model+year;
- пробег выглядит слишком низким или слишком высоким относительно аналогов;
- год выпуска не попадает в диапазон generation;
- свежие автомобили с очень большим пробегом.

Вероятно валидные редкие случаи:
- отличия кузова, объема двигателя и привода от доминирующего значения часто похожи на редкие комплектации или различия классификации, а не на явную ошибку.

3. Детальные находки

Отрицательный пробег
Пример: id 3, kolesa_id 218695550, Chevrolet Captiva 2021, пробег -100 км, цена 7150000.
ссылка

Невозможный год выпуска
Пример: id 2, kolesa_id 222486528, Chevrolet Monza, год выпуска 2035, поколение 2022 - н.в.. Этот же объект попал в кандидаты generation_year_mismatch.
ссылка

Неожиданное топливо
В fuel_type найдено значение пар в 1 объявлении: id 4, kolesa_id 220471159, Chevrolet Cruze 2013.
ссылка

Изображения
- found_img = true, но ссылка на изображение пустая: 1 объявление. Пример: id 75, kolesa_id 222889946.
ссылка
- found_img = false, но img_url заполнен: 81 объявление.
- imgs_count > 0, но found_img = false: 76 объявлений.
- imgs_count = 0, но found_img = true: 67 объявлений.

Кандидаты по цене
Custom SQL нашел объявления, где цена резко отличается от медианы аналогов:
- id 320, kolesa_id 222243157, Toyota Camry 2011: цена 45000000 против медианной цены 7300000.
ссылка
- id 448, kolesa_id 222075136, Hyundai Elantra 2023: цена 2300000 против медианной цены 8400000.
ссылка

Кандидаты по пробегу
Есть подозрительно низкие пробеги у старых авто:
- id 1795, kolesa_id 194957319, Chevrolet Cruze 2012: пробег 145 км при медианном пробеге группы 236000 км.
ссылка
- id 2100, kolesa_id 211009325, Audi A6 1997: пробег 196 км при медианном пробеге группы 315000 км.
ссылка

Свежие автомобили с большим пробегом
Кандидаты:
- id 5384, kolesa_id 222755699, Hyundai Sonata 2026: пробег 300000 км.
ссылка
- Mercedes-Benz Sprinter 2023 с пробегом 220000 км может быть валидным редким случаем для коммерческого авто, но требует проверки.

4. Что проверить вручную

- Исправить или отфильтровать отрицательный пробег и год выпуска 2035.
- Проверить источник значения fuel_type = пар.
- Сверить логику заполнения found_img, img_url и imgs_count.
- Проверить кандидатов с экстремальными ценами и пробегами.
- По кузову, объему двигателя и приводу проверять только выборочно: Kia Rio хэтчбек, Kia Cerato купе, BMW X5 4.4, Toyota Camry 3.5 похожи на валидные редкие категории или комплектации.

5. Итоговая оценка качества данных

Качество данных среднее: критичных ошибок немного, но есть системные расхождения по изображениям и заметные кандидаты на ошибки в цене, пробеге и поколении. Проверки kolesa_url_id_mismatch и fuel_engine_volume_mismatch кандидатов не нашли; связь электромобилей и engine_volume = 0 выглядит согласованной.
```

```text
Рассчитываю качество работы агента...
```

```text
Оценка качества поиска

Ожидалось аномалий: 9
Найдено ожидаемых: 7
Recall поиска: 77.8%
Всего findings агента: 223
Findings вне тестовой разметки, не участвующие в recall: 216
Пропущено: 2

Агент нашел 7 из 9 ожидаемых аномалий во внутренних findings. Пропущены контекстные несоответствия Lexus LX 570 с engine_volume=1.5 и BMW i3 как бензиновый автомобиль с engine_volume=2.0. Остальные стандартные проверки и ценовой/пробеговый контекст найдены корректно.

Качество итогового отчета

Показано ожидаемых аномалий: 6
Recall отчета: 66.7%
Не показано в отчете: 3

В итоговом отчете явно отражены 6 из 9 ожидаемых аномалий. Не видна конкретная строка found_img=false при заполненном img_url, так как дан только общий счетчик; также отсутствуют Lexus LX 570 с малым двигателем и BMW i3 с неверным топливом/двигателем.
```

### Ответ на запрос `/ask`

В данном кейсе пользователем был задан конкретный вопрос, для ответа на который агент сформировал один SQL-запрос. Если вопрос требует нескольких независимых выборок, граф может последовательно выполнить несколько запросов.

В бот было отправлено сообщение:

```text
/ask Есть ли в данных подозрительные объявления в соотношении цены и года автомобиля. То есть условно новые автомобили по низкой цене
```

Был получен следующий ответ:

```text
Да, подозрительные объявления есть.

Я искал относительно свежие авто, у которых цена сильно ниже рынка для той же марки, модели и года выпуска.  
Критерий аномалии: цена не выше 65% от медианной цены по аналогичным объявлениям и ниже нижнего квартиля. В сравнение попадали только группы, где есть минимум 5 похожих объявлений.

Главный вывод  
Найдено 7 объявлений за 2021–2023 годы, которые выглядят заметно дешевле аналогов. Для 2024 и 2025 годов таких случаев по выбранному критерию не найдено.

Наиболее подозрительные объявления

• Hyundai Elantra, 2023, Алматы  
Цена: 2 300 000 ₸  
Медиана по Hyundai Elantra 2023: 8 400 000 ₸  
Соотношение к медиане: 27.4%  
Пробег: 140 000 км  
Сравнимых объявлений: 17  
Открыть объявление  
Это самый сильный выброс: цена почти в 3.7 раза ниже медианы. Пробег большой для 2023 года, но разница всё равно выглядит аномальной.

• Kia K5, 2021, Астана  
Цена: 5 000 000 ₸  
Медиана по Kia K5 2021: 11 500 000 ₸  
Соотношение к медиане: 43.5%  
Пробег: 85 000 км  
Сравнимых объявлений: 13  
Открыть объявление  
Цена более чем в 2 раза ниже медианы для этой модели и года.

• Chevrolet Nexia, 2021, Усть-Каменогорск  
Цена: 2 000 000 ₸  
Медиана по Chevrolet Nexia 2021: 4 000 000 ₸  
Соотношение к медиане: 50.0%  
Пробег: 100 000 км  
Сравнимых объявлений: 40  
Открыть объявление  
Хорошая база сравнения — 40 объявлений. Цена ровно в 2 раза ниже медианы.

• Chevrolet Cobalt, 2022, Костанай  
Цена: 2 600 000 ₸  
Медиана по Chevrolet Cobalt 2022: 5 100 000 ₸  
Соотношение к медиане: 51.0%  
Пробег: 200 000 км  
Сравнимых объявлений: 47  
Открыть объявление  
Очень большой пробег для 2022 года может частично объяснять скидку, но цена всё равно вдвое ниже медианы.

• Hyundai Accent, 2021, Алматы  
Цена: 3 800 000 ₸  
Медиана по Hyundai Accent 2021: 7 400 000 ₸  
Соотношение к медиане: 51.4%  
Пробег: 130 000 км  
Сравнимых объявлений: 13  
Открыть объявление

• Toyota Camry, 2021, Шымкент  
Цена: 8 300 000 ₸  
Медиана по Toyota Camry 2021: 14 000 000 ₸  
Соотношение к медиане: 59.3%  
Пробег: 77 000 км  
Сравнимых объявлений: 23  
Открыть объявление  
Особенно стоит проверить: пробег не выглядит экстремально большим, но цена сильно ниже рынка для Camry 2021.

• Kia Cerato, 2021, Шымкент  
Цена: 4 860 000 ₸  
Медиана по Kia Cerato 2021: 7 700 000 ₸  
Соотношение к медиане: 63.1%  
Пробег: 158 542 км  
Сравнимых объявлений: 7  
Открыть объявление

Сводка по годам

• 2025: 95 сравнимых объявлений, подозрительных 0  
• 2024: 142 сравнимых объявления, подозрительных 0  
• 2023: 280 сравнимых объявлений, подозрительных 1 — 0.36%  
• 2022: 192 сравнимых объявления, подозрительных 1 — 0.52%  
• 2021: 259 сравнимых объявлений, подозрительных 5 — 1.93%  
• 2020: 130 сравнимых объявлений, подозрительных 2 — 1.54%  
• 2019: 123 сравнимых объявления, подозрительных 2 — 1.63%  
• 2018: 106 сравнимых объявлений, подозрительных 2 — 1.89%  
• 2017: 96 сравнимых объявлений, подозрительных 5 — 5.21%  
• 2016: 57 сравнимых объявлений, подозрительных 2 — 3.51%

Почему это может быть аномалией

Низкая цена сама по себе не доказывает мошенничество, но такие объявления стоит проверять вручную. Возможные причины:

• автомобиль после ДТП или с серьёзными повреждениями;  
• юридические ограничения, залог, кредит, арест;  
• скрученный или наоборот очень большой пробег;  
• автомобиль использовался в такси/каршеринге;  
• указана неполная цена, например первоначальный взнос;  
• ошибка в объявлении;  
• мошенническое объявление с целью получить предоплату.

Куда смотреть в первую очередь

Самые сильные кандидаты на проверку:

1. Hyundai Elantra 2023 за 2.3 млн ₸ — всего 27.4% от медианы.  
2. Kia K5 2021 за 5.0 млн ₸ — 43.5% от медианы.  
3. Toyota Camry 2021 за 8.3 млн ₸ — цена низкая при неэкстремальном пробеге 77 000 км.

Важно  
Анализ учитывал марку, модель, год и цену, но не учитывал комплектацию, двигатель, состояние кузова, историю ДТП, юридический статус и тип продажи. Поэтому результат лучше воспринимать как список объявлений для дополнительной проверки, а не как окончательный вывод о мошенничестве.
```

## Локальный запуск

Запуск происходит через Docker Compose. С отдельным поднятием БД и остальными сервисами.

### Подготовка

1. Скопировать `.env.example` в `.env`:

    *Mac/Linux:*

    ```bash
    cp .env.example .env
    ```

    *Windows:*

    ```bash
    copy .env.example .env
    ```

2. Вставить реальный `TG_TOKEN` в `.env`.

3. Вставьте OpenAI API ключ для взаимодействия с LLM от OpenAI (GPT).

4. Указать пароль для БД в `POSTGRES_PASSWORD`.

Настройки ответа на вопросы в `.env`:

- `ASK_SQL_QUERY_LIMIT` - максимум SQL-запросов, которые агент может выполнить для одного вопроса.

Настройки полного анализа в `.env`:

- `ANALYZE_DEFAULT_TABLE` - таблица для команды `/analyze`.
- `ANALYZE_MAX_CUSTOM_CHECK_ITERATIONS` - максимум итераций дополнительных SQL-проверок.
- `ANALYZE_CUSTOM_SQL_PER_ITERATION_LIMIT` - максимум custom SQL-запросов за одну итерацию.
- `ANALYZE_CUSTOM_SQL_TOTAL_LIMIT` - общий лимит custom SQL-запросов за анализ.
- `ANALYZE_REPORT_FINDINGS_LIMIT` - максимум findings, передаваемых в итоговый отчет.
- `ANALYZE_REPORT_SAMPLE_ROWS_LIMIT` - максимум примеров строк для одного finding.

### Запуск контейнеров

Далее этапы запуска через Docker Compose контейнеров отдельно для базы данных PostgreSQL и остальных сервисов (агент, бот).

### Запуск PostgreSQL

1. Запуск контейнера PostgreSQL:

    ```bash
    docker compose -f docker-compose.db.yaml up -d
    ```

2. Посмотреть логи базы:

    ```bash
    docker compose -f docker-compose.db.yaml logs -f db
    ```

3. Проверить статус:

    ```bash
    docker compose -f docker-compose.db.yaml ps
    ```

4. Подключиться к базе данных:

    Используйте значения `.env`
    По умолчанию:

      - Host: `localhost`
      - Port: `5432`
      - Database: `kolesa`
      - User: `kolesa`
      - Password: `установленный_в_.env`

5. Остановить контейнер с БД

    Обычная остановка:

    ```bash
    docker compose -f docker-compose.db.yaml down
    ```

    Удалить базу вместе с данными:

    ```bash
    docker compose -f docker-compose.db.yaml down -v
    ```

### Запуск остальных сервисов

Контейнер приложения запускается после PostgreSQL, поскольку при старте бот инициализирует БД и использует общую Docker-сеть

1. Запуск контейнера с выводом логов в терминал:

    Обычный запуск

    ```bash
    docker compose up --build
    ```

    Фоновый запуск:

    ```bash
    docker compose up --build -d
    ```

2. Посмотреть логи:

    ```bash
    docker compose logs -f
    ```

    Файловые логи сохраняются в `local/logs/logs.log`.

3. Проверить статус:

    ```bash
    docker compose ps
    ```

4. Остановить контейнер:

    ```bash
    docker compose down
    ```
